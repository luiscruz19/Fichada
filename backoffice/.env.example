# Puerto del panel
PORT=3000

# API de dominio (server-side; no se expone al browser)
API_URL=http://localhost:4000
# Servicio auth (server-side)
AUTH_URL=http://localhost:4001
# Basic Auth servicio↔servicio para hablar con auth (vive solo en el server)
AUTH_BASIC_USER=auth
AUTH_BASIC_PW=secret
