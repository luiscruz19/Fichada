node_modules/
*.log
npm-debug.log*

# Variables de entorno (se generan desde los .env.example)
.env
**/.env

# Deploy: los reales NO se versionan; los .example sí.
.env.deploy.*
!.env.deploy.example

# Build / OS
dist/
.DS_Store

# Firebase service account (SECRETO — nunca commitear)
sda-dev-*-firebase-adminsdk-*.json
*-firebase-adminsdk-*.json
**/google-services-account*.json
